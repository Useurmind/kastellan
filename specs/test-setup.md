I want a test environment where i can run the agent and the operator on my own machine, and can watch them interact.

Operator:
- runs in kind cluster
- reacts to a localhost

Agent 
- runs as pod on localhost