apiVersion: kastellan.useurmind.de/v1
kind: DockerStack
metadata:
  labels:
    app.kubernetes.io/name: kastellan
    app.kubernetes.io/managed-by: kustomize
  name: dockerstack-sample
spec:
  # TODO(user): Add fields here
